---
title: What is a DOM?
sidebar_position: 2
---

The **Document Object Model (DOM)** represents a document in logical tree. Branches of these trees ends in a node, where each nodes contains an object. We can use DOM methods allows you to access the tree programmatically. 

The DOM connects web pages to programming languages or scripts by representing the structure of a document in memory.